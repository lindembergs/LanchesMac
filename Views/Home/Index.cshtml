@model HomeViewModel

<partial name="_Carrossel"></partial>

<h2>Os preferidos da semana</h2>
<h4>Tá com fome ???</h4>

<div class="row">
    @foreach(var lanche in Model.LanchesPreferidos){
        <partial name="_LanchesResumo" model="@lanche" />
    }
</div>
